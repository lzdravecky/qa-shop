# QA Shop v1.0

Local system-under-test for the `playwright-ai-qa-lab`.

## Run

Requires Node.js 20+.

```bash
npm start
```

Open `http://localhost:3000`.

No dependencies and no `npm install` are required for v1.0.
